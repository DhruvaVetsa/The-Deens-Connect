<%- include("includes/settings-header.ejs") %>
    <!-- <style>
        .card {
            box-shadow: 0 4px 8px 0 rgba(0, 0, 0, 0.2);
            max-width: 300px;
            margin: auto;
            text-align: center;
        }

        .title {
            color: grey;
            font-size: 18px;
        }

        button {
            border: none;
            outline: 0;
            display: inline-block;
            padding: 8px;
            color: white;
            background-color: #000;
            text-align: center;
            cursor: pointer;
            width: 100%;
            font-size: 18px;
        }

        a {
            text-decoration: none;
            color: black;
        }

        button:hover,
        a:hover {
            opacity: 0.7;
        }

        .badge {
            background-color: gray;
            color: white;
            padding: 4px 8px;
            text-align: center;
            border-radius: 5px;
        }
    </style>
    <h1 style="text-align: center;">General Settings</h1>
    <div class="card">
        <img src="<%= userDATA.dpImageDataURL %>" alt="<%= userDATA.username %>" title="<%= userDATA.name %>"
            style="width:100%">
        <h1>
            <%= userDATA.username %>
        </h1>
        <p class="title">Class <%= userDATA.class_and_section %>
        </p>
        <p>
            <i class="fa-solid fa-<%= occupation %>"></i>
        </p>
        <% if (userDATA.profileDetails.discordName !="" || userDATA.profileDetails.discord !=undefined ) { %>
            <a title="<%= userDATA.profileDetails.discordName %>#<%= userDATA.profileDetails.discordID %>"><i
                    class="fa-brands fa-discord"></i></a>
            <% } %>
                <% if (userDATA.profileDetails.youtubeChannel !="" || userDATA.profileDetails.youtubeChannel !=undefined
                    ) { %>
                    <a href="<%= userDATA.profileDetails.youtubeChannel %>"><i class="fa-brands fa-youtube"></i></a>
                    <% } %>
                        <% if (userDATA.profileDetails.skype !="" || userDATA.profileDetails.skype !=undefined ) { %>
                            <a title="<%= userDATA.profileDetails.skype %>"><i class="fa-brands fa-skype"></i></a>
                            <% } %>
                                <% if (userDATA.profileDetails.github !="" || userDATA.profileDetails.github !=undefined
                                    ) { %>
                                    <a href="<%= userDATA.profileDetails.github %>"><i
                                            class="fa-brands fa-github"></i></a>
                                    <% } %>
                                        <% if (userDATA.profileDetails.insta !="" || userDATA.profileDetails.insta
                                            !=undefined ) { %>
                                            <a href=" <%= userDATA.profileDetails.insta %>"><i
                                                    class="fa-brands fa-square-instagram"></i></a>
                                            <% } %>
                                                <% if (userDATA.profileDetails.fb !="" || userDATA.profileDetails.fb
                                                    !=undefined ) { %>
                                                    <a href="<%= userDATA.profileDetails.fb %>"><i
                                                            class="fa-brands fa-facebook"></i></a>
                                                    <% } %>
                                                        <p><button><span class="badge">aka</span>
                                                                <%= userDATA.name %>
                                                            </button></p>
    </div> -->
    <div class="profile">
        <br><br>
        <div id="overlay-solid-color" ondblclick="window.location.replace('#profileBannerSolidColorLB')"></div>
        <!-- Light Boxes -->
        <div id="profileBannerSolidColorLB" class="enlargePic">
            <form action="/settings/change-profile-banner-colour" method="post">
                <input type="color" name="bannerColor" value="<%= userDATA.profileBannerColour %>">
                <button class="closeLB" type="submit">&times;</button>
            </form>
        </div>
    </div>
    <%- include("includes/settings-footer.ejs") %>